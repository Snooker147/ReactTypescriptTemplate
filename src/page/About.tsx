import * as React from "react";

// Props Structure
export interface AboutProps { }

export default (p: AboutProps) => {
    return (
        <div>
            About Component
        </div>
    );
};