import * as React from "react";

// Props Structure
export interface NotFoundProps { }

export default (p: NotFoundProps) => {
    return (
        <div>
            Page Not Found!
        </div>
    );
};